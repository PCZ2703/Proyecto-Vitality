class Pago {
    constructor(usuario, monto, metodoPago, estado, referenciaSuscripcion) {
        this.usuario = usuario;
        this.monto = monto;
        this.metodoPago = metodoPago;
        this.estado = estado;
        this.referenciaSuscripcion = referenciaSuscripcion;
    }
}